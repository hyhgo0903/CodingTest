#include <vector>
#include <algorithm>
#include <iostream>
#include <map>
#include <string>
using namespace std;

class Implementation
{
public:
  void UDLR(int square, string commandList);
  void TimeInclude3(int number);
  void ChessKnight(string str);
  void Map(int x, int y, int startX, int startY, int startDir, int number[]);
};