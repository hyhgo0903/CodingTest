#include <vector>
#include <algorithm>
#include <iostream>
using namespace std;

class Changes
{
public:
    void Start(int);
};

class LawOfGreatNumbers
{
public:
    // n은 필요하지 않아 생략했음
    void Start(int, int, vector<int>);
};