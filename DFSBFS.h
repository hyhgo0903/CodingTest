#include <queue>
#include <stack>
#include <string>
#include <iostream>
using namespace std;

class DFSBFS
{
public:
  void ZeroSpace(int x, int y, int arr[]);
  void Maze(int x, int y, int arr[]);
};